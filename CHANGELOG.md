# 更新记录

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；版本号跟 `docs/安装说明.md` 顶部一致。

## [1.0] - 2026-09-22

第一个公开版本。两个运行文件（`src/mc-backup.js` + `web/card-backup.html`）+ 一份说明，只依赖 Node，Windows / Linux 通用。

### 备份

- **增量快照**：默认每 10 分钟一份，只写变化的文件，没变的用硬链接复用（几十 GB 的整合包也是秒级）
- **自动存档对齐系统整刻**：10 分钟档 = 12:00 / 12:10 / 12:20…；2 小时档 = 12:00 / 14:00…，不会因为程序何时启动而一直偏着几分钟
- **分档保留，一档两列**：`增量留几份 / 完整留几份`，**填 0 就是不存**；粗档按自然时间段各钉一份，10 分钟档转多少圈都挤不掉它
- **手动档独立**：面板点出来的快照标为手动档，不进自动保存循环、不被自动时间段轮转清掉
  - **立刻保存（增量）** = 把变化写进**最近那份手动档**（不多出一份；没变的保持、源里删掉的也删掉）
  - **全部保存（完整）** = 另起一份独立的完整复制，可以单独拿走；之后点立刻保存就是刷新这一份
  - 手动档默认最多留 20 份（`manualKeep`）
- **快照前先 save-all**，快照先写 `.tmp` 再改名（半成品不会被当成有效快照）

### 回档与导出

- 粒度：整个存档 / 单个世界 / 单个玩家；进阶里可按坐标**只回档一个区块**或**整个区域**，还能查该区块在各快照里的变化历史
- 导出：整份存档 / 单个世界 / 单个玩家 / 整个服务端 / **纯净服务端**（不含存档和玩家）；玩家数据可选带全部 / 不带 / 只带某一个
- 回档、玩家数据转移前**自动停服 + 把当前文件另存一份**（`_manual/`），做完自动开服

### 面板卡片

- 主界面只留「选服务端 → 选目标 → 选存档 → 还原」+ 保存 + 导出；坐标/区块回档、删除存档（两次确认）、玩家数据转移收进「高级功能」
- 进度条（进行中蓝色 / 完成绿色保留 90 秒）、下次自动保存时间、本机系统时间
- 存档下拉按「手动 → 完整档位 → 增量档位」分组；时间用**分钟 / 小时 / 天**下拉选，不用自己换算
- 全局设置 + 单个服务端单独设置

### 兼容与适配

- **GTNH**：大写 `World` 目录、`players/名字.dat`、个人维度 `DIM-xxx`、`serverutilities` / `journeymap` / `java9args.txt` / `startserver-java9.bat` 等
- 世界类型自动标注（主世界 / 下界 / 末地 / 个人维度 / 平坦 / 虚空），可给世界写备注
- 纯 Node 实现，无第三方依赖；一键安装 `node mc-backup.js install --yes`

### 测试

- `tests/sandbox-test.js` **92 项**（真实引擎 + 假 GTNH 服务端，跑完面板会发的全部请求）
- `tests/chain-test.js` **20 项**（自动/手动档语义、刷新、保留策略）
- `tests/card-test.js` **47 项**（卡片脚本，假 DOM）
- `tests/retention-test.js`（时间段锚点对比）
- CI：Ubuntu × Node 18 / 20 / 22
